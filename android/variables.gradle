ext {
    minSdkVersion = 24

    // لازم تترفع عشان androidx.browser 1.9.0
    compileSdkVersion = 36
    targetSdkVersion = 36

    androidxActivityVersion = '1.10.1'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.15.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'

    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'

    cordovaAndroidVersion = '14.0.1'

    // أحدث إصدار من Google Mobile Ads SDK (كان متسيب على 24.9.+ الافتراضية
    // بتاعة البلجن، وهي أقدم من أحدث إصدار فعلي متاح — وده كان سبب تحذير
    // AdMob "يجب تثبيت أحدث إصدار من حزمة SDK لإعلانات Google الجوّالة").
    playServicesAdsVersion = '25.0.0'
}
