-- ============================================================
-- الانتقال من كاشير (Kashier) إلى بايموب (Paymob) كبوابة الدفع:
--   - نفس منطق المنتجين ونفس عمودَي profiles بالظبط
--     (purchased_reward_unlock / purchased_no_ads) — مفيش أي تغيير
--     في منطق فتح المزايا نفسه.
--   - بس بنعيد تسمية عمود المرجع في جدول purchases عشان يبقى عام
--     (gateway_reference) بدل ما يفضل مربوط باسم كاشير تحديدًا،
--     دلوقتي بيتخزّن فيه معرّف معاملة بايموب (obj.id) بعد التأكيد.
-- ============================================================

ALTER TABLE public.purchases
  RENAME COLUMN kashier_reference TO gateway_reference;
